[project]
name = "open-experiments"
version = "0.1.0"
description = ""
authors = [
    {name = "Your Name",email = "you@example.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "datahub (>=0.999.1,<0.1000.0)",
    "owslib (>=0.34.1,<0.35.0)"
]

[tool.poetry]
packages = [{include = "open_experiments", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
